import { createContext } from 'react';

const githubContext = createContext();

export default githubContext;

//Creating the context for universal store
//createContext
//name of context githubContext
